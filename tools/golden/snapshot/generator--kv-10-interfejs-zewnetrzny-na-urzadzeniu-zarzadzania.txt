A "Skip to content"
SPAN "Rackpathlabs"
SMALL "OpenStack tooling"
A "start"
A "globals.yml"
A "inventory validator"
H1 "generator for Kolla-Ansible"
CODE "globals.yml"
P "Fill in the deployment parameters — the file assembles live on the right. Settings are saved locally in the browser; nothing leaves this machine."
H2 "Deployment parameters"
LEGEND "Import an existing file"
P "Paste or drop a . The form fills with the values from the file, and from then on the export instead of rewriting it — comments, key order and entries the generator does not know stay untouched."
CODE "globals.yml"
STRONG "edits the original"
BUTTON "Load from the field"
BUTTON "Load a file…"
BUTTON "Discard the import"
LEGEND "Base image"
LABEL "kolla_base_distro"
SPAN "required"
P "Base distribution of the container images. Must match the images in the registry."
LABEL "openstack_release"
SPAN "required"
P "Image tag, for example . Should match the kolla-ansible version."
CODE "2025.1"
LEGEND "Networking"
LABEL "kolla_internal_vip_address"
SPAN "required"
P "A free IPv4 address on the management network — not held by any host."
LABEL "network_interface"
SPAN "required"
P "Management/API network interface. Needs an IP address on every node."
LABEL "neutron_external_interface"
SPAN "required"
P "External traffic interface. No IP address, no configuration — Neutron takes it over entirely. Several comma-separated interfaces mean several physical networks."
LABEL "neutron_bridge_name"
P "OVS bridges, one per external interface. The counts must match."
LABEL "api_interface"
P "API traffic and the Corosync ring. Empty inherits ."
CODE "network_interface"
LABEL "storage_interface"
P "Storage traffic (Ceph). Empty inherits ."
CODE "network_interface"
LABEL "migration_interface"
P "Live migration. Empty inherits ."
CODE "api_interface"
LABEL "kolla_external_vip_address"
P "Address of the public endpoints. Empty means the same as internal."
LABEL "kolla_external_vip_interface"
P "Interface that carries the external VIP."
LEGEND "Names and TLS"
LABEL "kolla_internal_fqdn"
P "Name of the internal endpoints in the service catalogue."
LABEL "kolla_external_fqdn"
P "Name of the public endpoints. Must differ from the internal one."
SPAN "kolla_enable_tls_internal"
SPAN "TLS on internal traffic between services."
SPAN "kolla_copy_ca_into_containers"
SPAN "Copies a private CA into the container trust store."
SPAN "kolla_enable_letsencrypt"
SPAN "Let's Encrypt certificates — the images' system trust store suffices."
LEGEND "Services"
LABEL "neutron_plugin_agent"
P "ML2 backend. OVN and openvswitch are mutually exclusive."
LABEL "storage backend"
P "Source of volumes and images. Kolla does not deploy Ceph — the cluster must already exist."
SPAN "enable_haproxy"
SPAN "HAProxy + keepalived — required for the VIP and HA API endpoints."
SPAN "enable_cinder"
SPAN "Block volumes. Needs a selected storage backend."
SPAN "enable_heat"
SPAN "Template orchestration."
SPAN "enable_horizon"
SPAN "Web dashboard."
SPAN "enable_barbican"
SPAN "Secret store. Octavia needs it for the amphora certificates."
SPAN "enable_octavia"
SPAN "Load balancing (amphorae). Needs Barbican and a management network."
SPAN "enable_prometheus"
SPAN "Metrics collection. Needs hosts in the group."
EM "monitoring"
SPAN "enable_grafana"
SPAN "Metric dashboards. Without Prometheus there is nothing to draw."
SPAN "enable_hacluster"
SPAN "Corosync and Pacemaker. The Corosync ring runs over ."
CODE "api_interface"
SPAN "enable_masakari"
SPAN "Automatic evacuation of VMs after a host failure."
SPAN "enable_neutron_provider_networks"
SPAN "Provider networks (flat/VLAN) mapped straight onto the physical network."
LEGEND "Octavia"
CODE "octavia_amp_network"
SPAN "type"
P "Amphora management network. and need a provider network."
CODE "vlan"
CODE "flat"
LABEL "provider_physical_network"
P "Physical network name. Must follow from the external interface list."
LEGEND "Deliberate decisions"
P "Some rules describe a risk the file alone cannot settle. An acknowledgment does not silence the finding — it lowers it to information and records the decision as a comment in the generated file."
SPAN "the shared link has headroom and QoS"
SPAN "Measured bandwidth headroom and switch-side queueing for storage and migration traffic."
SPAN "migration has its own network outside Kolla"
SPAN "A dedicated migration network configured through in a nova-compute override."
CODE "live_migration_inbound_addr"
LEGEND "Output"
SPAN "Explanatory comments"
SPAN "Section headers and deployment notes that follow from the chosen configuration."
BUTTON "Restore defaults"
SPAN "/etc/kolla/globals.yml"
SPAN "full file"
SPAN "only what differs"
BUTTON "Copy"
BUTTON "Copy link"
BUTTON "Download globals.yml"
H2 "Configuration check"
SPAN "This file is not ready to deploy: 1 error, 2 warnings. Export is blocked."
SPAN "error"
SPAN "Field is required."
CODE "kolla_internal_vip_address"
SPAN "warning"
SPAN "() and () sit on the same device . Corosync takes its ring from . A Ceph backfill, an OSD-replace rebalance or a single 64 GB migration can saturate the link; Corosync loses its token (3000 ms by default), Pacemaker declares the node lost, and Masakari starts evacuating a healthy host under load. The classic “everything worked for three weeks and then the cloud killed its own compute node”."
CODE "storage_interface"
CODE "bond0.10"
CODE "api_interface"
CODE "bond0.10"
CODE "bond0"
CODE "api_interface"
SPAN "warning"
SPAN "() shares a base device with (). Kolla plugs the interface into an OVS bridge and strips its addressing. On a direct match you lose the host mid-deploy, at the task. On a shared base device the VLAN subinterface theoretically survives its parent joining OVS, but that depends on initialisation order at boot — the deployment passes and the host does not come back after a reboot."
CODE "neutron_external_interface"
CODE "bond0"
CODE "network_interface"
CODE "bond0.10"
CODE "neutron : Bootstrap"
SPAN "info"
SPAN "Host systems supported in release : CentOS Stream 9, CentOS Stream 10, Debian Bookworm (12), Rocky Linux 9, Rocky Linux 10, Ubuntu Noble (24.04)."
CODE "2025.1"
SPAN "info"
SPAN "is set explicitly in the file — the value no longer depends on the release default (RabbitMQ stream fanout queues). With the default of , every stream leader can end up on one broker. On three control nodes it is worth checking how the leaders are distributed once the deployment is up."
CODE "om_enable_rabbitmq_stream_fanout"
CODE "true"
SPAN "info"
SPAN "is unset — migration goes over (). A host-failure evacuation is a burst of migrations at exactly the moment the control plane is busiest. Migration traffic starves RPC, on healthy hosts misses heartbeats (, 60 s by default), Nova marks them down, and Masakari sees more hosts to evacuate. Avalanche: one host failure takes down the cluster."
CODE "migration_interface"
CODE "api_interface"
CODE "bond0.10"
CODE "nova-compute"
CODE "service_down_time"
FOOTER "Rackpathlabs — OpenStack tools. A single HTML file, no external dependencies, works offline."
ARIA-LABEL "Tools"
ARIA-LABEL "Theme: following the system setting"
PLACEHOLDER "--- kolla_base_distro: \"rocky\" …"
ARIA-LABEL "Contents of the globals.yml file to import"
PLACEHOLDER "2025.1"
PLACEHOLDER "10.0.0.250"
PLACEHOLDER "eth0"
PLACEHOLDER "eth1"
PLACEHOLDER "br-ex"
PLACEHOLDER "inherits network_interface"
PLACEHOLDER "inherits network_interface"
PLACEHOLDER "inherits api_interface"
PLACEHOLDER "same as internal"
PLACEHOLDER "inherits network_interface"
PLACEHOLDER "VIP address by default"
PLACEHOLDER "VIP address by default"
PLACEHOLDER "physnet1"
ARIA-LABEL "Output view"
TITLE "Fix the configuration errors to export the file"
TITLE "Puts the settings into the address bar and copies the link. Settings only — a pasted globals.yml is never encoded."
TITLE "Fix the configuration errors to export the file"
ARIA-LABEL "Preview of the generated globals.yml"
